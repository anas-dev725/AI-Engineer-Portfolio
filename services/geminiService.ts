// This service has been disabled as per request to remove API key usage.
// To re-enable AI features, reinstall @google/genai and restore the service logic.

export const analyzeMessage = async (message: string) => {
  console.log("AI Analysis is currently disabled.");
  return null;
};